import SwiftUI

// Palette de marque — la source unique du miel de Cubby.
// Les mêmes valeurs que l'icône (Logo/make-icon.swift) : dégradé #F2B24A → #C2711A.
extension Color {
    /// Miel Cubby — couleur d'accent de toute l'app (état actif, sélection, progression).
    static let cubby = Color(.sRGB, red: 0.949, green: 0.698, blue: 0.290, opacity: 1)
}
